server:
  port: 8082

spring:
  application:
    name: account-service
  config:
    import: optional:configserver:http://localhost:9999 # server from were to get the rest of configuration

  h2:
    console:
      enabled: true